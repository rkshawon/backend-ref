
import roomMessages from "./room-message.controller";
import createMessage from "./create-message.controller"
import subscribe from "./subscribe.controller"

const MessageController={
    roomMessages,
    createMessage,
    subscribe
}

export default MessageController